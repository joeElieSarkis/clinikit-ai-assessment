# Assessment data

Place the supplied `CliniKit_NoShow_Dataset.csv` here, or pass its path with `--data`.
The source CSV is excluded from Git. No external or replacement dataset is used.

The training result records its SHA-256 digest so another run can verify the input.
